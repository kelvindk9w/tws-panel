{
  "modulo": "email",
  "titulo": "E-mail",
  "resumo": "Servidor de e-mail próprio (Stalwart Mail Server em container Docker) gerenciado pelo painel: domínios com DKIM, checklist/verificação de DNS, caixas de e-mail (IMAP/SMTP) e injeção automática de credenciais SMTP nos projetos deployados. Todas as rotas exigem sessão (ou setup token enquanto o setup não terminou), via o guard global de auth.",
  "pontoDeEntrada": { "api": "GET /api/mail/status", "web": "/mail" },
  "comoFunciona": [
    "O painel gerencia um único container Docker dedicado (paas-stalwart) rodando a imagem stalwartlabs/mail-server:v0.11.8 (Stalwart Mail Server, linha v0.11.x — a última com API REST de gerenciamento; a linha nova stalwartlabs/stalwart v0.16+ removeu essa API e exige wizard interativo, então NÃO é usada aqui). O container roda na rede Docker paas-net, a mesma rede dos projetos deployados.",
    "Persistência: os dados internos do Stalwart (contas, mensagens IMAP, chaves privadas DKIM) ficam no volume Docker nomeado paas_stalwart_data, montado em /opt/stalwart-mail/data. O painel injeta a configuração (data/mail/stalwart/config.toml, renderizada a cada start) montada somente-leitura em /opt/stalwart-mail/etc.",
    "1º uso: o usuário clica 'Iniciar servidor' (POST /api/mail/server/start). Se ainda não existe, o painel gera um adminSecret aleatório (24 bytes → base64url) e o persiste em data/mail/mail.json (0600). Escreve config.toml com esse secret como [authentication.fallback-admin], sobe o container (docker run, --restart unless-stopped) e espera a API HTTP interna (porta 8080 por padrão, mapeada para o host) responder (200 ou 401).",
    "O painel fala com o Stalwart via API REST HTTP Basic (usuário 'admin', senha = adminSecret) apontando para http://127.0.0.1:<porta-http> — ou seja, sempre a partir do HOST onde o painel roda, nunca via nome de rede Docker (StalwartClient em packages/mailer/src/client.ts).",
    "Adicionar um domínio (POST /api/mail/domains): o painel cria o domínio no Stalwart (POST /api/principal {type:'domain'}), gera um par de chaves DKIM RSA 2048 (POST /api/dkim {algorithm:'Rsa'}), lê a chave pública (GET /api/dkim/{id}) e guarda dkimPublicKey/dkimSelector('paas')/dkimKeyBits(2048) em data/mail/mail.json. Cria automaticamente a caixa postmaster@<domínio> (com alias abuse@) — exigida pelas boas práticas de e-mail.",
    "Checklist de DNS (GET /api/mail/domains/:domain/dns): o painel MONTA localmente (sem consultar o Stalwart nem o DNS real) a lista de registros esperados — A (mail.<domínio>), MX, SPF (v=spf1 ip4:<ip> ~all/-all conforme o estágio DMARC), DKIM (TXT com a chave pública gerada) e DMARC (progressivo none→quarantine→reject). O usuário copia cada registro para o provedor de DNS do domínio.",
    "Verificar DNS (POST /api/mail/domains/:domain/verify): consulta o DNS real via resolver público injetável (1.1.1.1/8.8.8.8, não o resolver local da VPS) e compara cada registro com o esperado (found/missing/mismatch). Verifica também o reverse DNS (PTR) do IP público — que o painel NÃO consegue configurar (depende do provedor da VPS) — e, se ausente/divergente, gera um texto de chamado pronto para abrir suporte. O resultado (ok/total) é persistido em domain.lastVerify.",
    "Criar caixa de e-mail (POST /api/mail/domains/:domain/mailboxes): gera senha forte (ou usa a informada, mín. 8 chars) e cria a caixa no Stalwart como principal 'individual' com role 'user' (obrigatório — sem essa role o Stalwart autentica mas nega SMTP/IMAP). A senha é guardada em CLARO em data/mail/mail.json e devolvida ao cliente apenas na criação (depois só via /credentials).",
    "Habilitar e-mail para um projeto (POST /api/projects/:id/email): cria (se não existir) uma caixa técnica <slug-do-projeto>@<domínio> e registra a associação projeto→caixa em data/mail/mail.json. No boot do módulo, mail.ts chama app.deployService.setEnvProvider(service.envForProject) — isso conecta o módulo de e-mail ao engine de deploy da Fase 2: a cada deploy do projeto, o engine chama envForProject(project) e injeta SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASS/MAIL_FROM como variáveis de ambiente do container (compose: em TODOS os serviços do compose.yml; dockerfile: no container único via -e; site estático: injeção ignorada, apenas loga um aviso).",
    "O host injetado é sempre o alias de rede Docker 'paas-stalwart' na porta 587 (submission, STARTTLS) — funciona porque o container do projeto e o container do Stalwart compartilham a rede paas-net. O certificado é autoassinado (sem seção [certificate.*] no config.toml), então clientes SMTP devem desabilitar a verificação estrita do TLS (ex.: nodemailer tls.rejectUnauthorized:false).",
    "Blacklist (GET /api/mail/blacklist): consulta DNSBLs públicas (Spamhaus ZEN, SpamCop, Barracuda para o IP; Spamhaus DBL para cada domínio cadastrado) via reverse-DNS lookup (127.0.0.0/8 = listado)."
  ],
  "arquivosFonte": {
    "rotas": ["apps/server/src/routes/mail.ts"],
    "servicos": ["apps/server/src/services/mail-service.ts"],
    "pacotes": [
      "packages/mailer/src/server.ts",
      "packages/mailer/src/client.ts",
      "packages/mailer/src/mailboxes.ts",
      "packages/mailer/src/dns-checklist.ts",
      "packages/mailer/src/blacklist.ts",
      "packages/mailer/src/smtp-inject.ts",
      "packages/mailer/src/exec.ts",
      "packages/mailer/src/index.ts",
      "packages/core/src/mail.ts",
      "packages/deploy/src/engine.ts"
    ],
    "web": ["apps/web/src/pages/MailPage.tsx", "apps/web/src/pages/MailDomainPage.tsx"],
    "testes": [
      "packages/mailer/tests/mailboxes.test.ts",
      "packages/mailer/tests/blacklist.test.ts",
      "packages/mailer/tests/dns-checklist.test.ts",
      "apps/web/tests/mail-domain-page.test.tsx",
      "scripts/test-mail.mts"
    ]
  },
  "persistencia": [
    {
      "arquivo": "data/mail/mail.json",
      "formato": "json (chmod 0600)",
      "conteudo": "{ adminSecret: string|null, hostname: string|null, domains: Record<domínio, {name, dkimSelector, dkimPublicKey, dkimKeyBits, dmarcStage, createdAt, lastVerify}>, mailboxes: Record<email, {id, localPart, domain, kind:'user'|'project'|'system', createdAt, password}>, projects: Record<projectId, {domain, mailbox, enabledAt}> }. IMPORTANTE: mailboxes[].password fica em TEXTO PLANO (necessário para reexibir credenciais e para a injeção SMTP)."
    },
    {
      "arquivo": "data/mail/stalwart/config.toml",
      "formato": "toml (chmod 0600, regenerado a cada start)",
      "conteudo": "Config mínima e determinística do Stalwart: hostname, listeners (smtp:25, submission:587, submissions:465 TLS implícito, imap:143, imaptls:993 TLS implícito, http:8080), [authentication.fallback-admin] user=admin secret=<adminSecret>, storage 100% rocksdb, sem seção [certificate.*] (certificado autoassinado gerado pelo próprio Stalwart)."
    },
    {
      "arquivo": "volume Docker paas_stalwart_data",
      "formato": "rocksdb (interno ao Stalwart)",
      "conteudo": "Todo o estado real do servidor de e-mail: contas, mensagens IMAP, filas SMTP e as CHAVES PRIVADAS DKIM (o painel só guarda a chave pública). Não é um arquivo local — só existe dentro do Docker; perder o volume derruba tudo mesmo com mail.json intacto."
    }
  ],
  "endpoints": [
    { "id": "status", "metodo": "GET", "url": "/api/mail/status", "resumo": "Estado do servidor de e-mail (instalado/rodando/versão/portas).", "arquivo": "status.json" },
    { "id": "iniciar-servidor", "metodo": "POST", "url": "/api/mail/server/start", "resumo": "Provisiona/inicia o container do Stalwart.", "arquivo": "iniciar-servidor.json" },
    { "id": "parar-servidor", "metodo": "POST", "url": "/api/mail/server/stop", "resumo": "Para o container do Stalwart.", "arquivo": "parar-servidor.json" },
    { "id": "listar-dominios", "metodo": "GET", "url": "/api/mail/domains", "resumo": "Lista os domínios de e-mail cadastrados.", "arquivo": "listar-dominios.json" },
    { "id": "criar-dominio", "metodo": "POST", "url": "/api/mail/domains", "resumo": "Provisiona um domínio: DKIM RSA 2048 + caixa postmaster@.", "arquivo": "criar-dominio.json" },
    { "id": "remover-dominio", "metodo": "DELETE", "url": "/api/mail/domains/:domain", "resumo": "Remove um domínio e todas as suas caixas.", "arquivo": "remover-dominio.json" },
    { "id": "blacklist", "metodo": "GET", "url": "/api/mail/blacklist", "resumo": "Checa o IP público e os domínios contra DNSBLs.", "arquivo": "blacklist.json" },
    { "id": "dns-checklist", "metodo": "GET", "url": "/api/mail/domains/:domain/dns", "resumo": "Lista os registros DNS esperados para o domínio.", "arquivo": "dns-checklist.json" },
    { "id": "verificar-dns", "metodo": "POST", "url": "/api/mail/domains/:domain/verify", "resumo": "Verifica os registros DNS contra o DNS real + PTR.", "arquivo": "verificar-dns.json" },
    { "id": "listar-caixas", "metodo": "GET", "url": "/api/mail/domains/:domain/mailboxes", "resumo": "Lista as caixas de e-mail do domínio (sem senha).", "arquivo": "listar-caixas.json" },
    { "id": "criar-caixa", "metodo": "POST", "url": "/api/mail/domains/:domain/mailboxes", "resumo": "Cria uma caixa de e-mail (IMAP/SMTP) no domínio.", "arquivo": "criar-caixa.json" },
    { "id": "remover-caixa", "metodo": "DELETE", "url": "/api/mail/domains/:domain/mailboxes/:id", "resumo": "Remove uma caixa de e-mail do tipo 'user'.", "arquivo": "remover-caixa.json" },
    { "id": "credenciais-caixa", "metodo": "GET", "url": "/api/mail/mailboxes/:id/credentials", "resumo": "Bloco de credenciais IMAP/SMTP pronto para cliente externo.", "arquivo": "credenciais-caixa.json" },
    { "id": "email-projeto", "metodo": "GET", "url": "/api/projects/:id/email", "resumo": "Configuração de e-mail atual de um projeto (env mascaradas).", "arquivo": "email-projeto.json" },
    { "id": "habilitar-email-projeto", "metodo": "POST", "url": "/api/projects/:id/email", "resumo": "Ativa e-mail para o projeto (cria caixa técnica e liga a injeção SMTP).", "arquivo": "habilitar-email-projeto.json" },
    { "id": "desabilitar-email-projeto", "metodo": "DELETE", "url": "/api/projects/:id/email", "resumo": "Desativa a injeção SMTP no próximo deploy do projeto.", "arquivo": "desabilitar-email-projeto.json" }
  ],
  "dependenciasGlobais": ["global/auth-plugin.json"],
  "pontosDeAtencao": [
    "Senhas de caixas de e-mail e o adminSecret do Stalwart ficam em TEXTO PLANO em data/mail/mail.json (só protegido por chmod 0600) — qualquer leitura desse arquivo (backup mal protegido, outro processo do mesmo usuário do SO) expõe todas as credenciais SMTP/IMAP e o acesso admin ao Stalwart.",
    "A imagem usada (stalwartlabs/mail-server:v0.11.8) é uma linha antiga/descontinuada — o próprio código (packages/mailer/src/server.ts:7-11) documenta que a linha nova (stalwartlabs/stalwart v0.16+) removeu a API REST usada por todo o módulo; migrar de versão exigiria reescrever client.ts inteiro. Dado novo verificado nesta rodada: CVE-2025-61600 (DoS não autenticada no parser IMAP do Stalwart) foi corrigida só na 0.13.4, SEM backport para a linha 0.11.x — a versão fixada aqui é vulnerável e permanece assim enquanto não migrar. Ainda aberto, ver global/review-2026-08-24.json (achado stalwart-descontinuado).",
    "Não existe endpoint para trocar a senha de uma caixa já criada — só criar (com senha opcional) ou remover; 'resetar senha' na prática exige remover e recriar a caixa.",
    "dnsChecklist() e listMailboxes() nunca chamam requireRunning(): o checklist é montado 100% localmente (não bate no Stalwart) e a lista de caixas lê só o arquivo local mail.json — então ambos respondem normalmente mesmo com o servidor de e-mail parado, mas podem ficar dessincronizados se o volume paas_stalwart_data for perdido/recriado sem que mail.json seja limpo.",
    "O painel só fala com o Stalwart via http://127.0.0.1:<porta> (loopback do host) — não há suporte a um Stalwart rodando em outra máquina.",
    "Certificado TLS autoassinado (sem ACME/Let's Encrypt) — SMTP_HOST/IMAP exigem desabilitar a verificação estrita do certificado no cliente, um padrão perigoso se replicado sem cuidado fora do e-mail interno projeto→Stalwart.",
    "O 'DMARC progressivo' (none → quarantine → reject) sugerido na UI (checklist.suggestion) é só texto: dmarcStage é gravado como 'none' na criação do domínio (mail-service.ts:251) e NUNCA é atualizado em lugar nenhum do código — não existe endpoint para avançar o estágio. Na prática todo domínio fica para sempre em SPF '~all'/DMARC 'p=none', a menos que o operador edite data/mail/mail.json manualmente."
  ]
}
