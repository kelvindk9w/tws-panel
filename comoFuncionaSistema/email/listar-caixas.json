{
  "id": "listar-caixas",
  "modulo": "email",
  "metodo": "GET",
  "url": "/api/mail/domains/:domain/mailboxes",
  "resumo": "Lista as caixas de e-mail de um domínio (sem a senha).",
  "arquivoFonte": "apps/server/src/routes/mail.ts:155",
  "autenticacao": {
    "exigida": true,
    "tipo": "sessao",
    "detalhe": "Guard global (apps/server/src/plugins/auth.ts): sessão via cookie após o setup; setup token enquanto pendente."
  },
  "rateLimit": "Global: 200 req/min por IP (RATE_LIMIT_MAX, padrão 200).",
  "parametros": {
    "path": [{ "nome": "domain", "tipo": "string", "obrigatorio": true, "regras": "schema Fastify (domainParamSchema, mail.ts): minLength 1, maxLength 253, pattern de domínio (MAIL_DOMAIN_SCHEMA) — NOVO: antes qualquer string era aceita (a rota nem valida existência, só filtra); agora um :domain fora do padrão de hostname é recusado antes do handler.", "descricao": "Nome do domínio (não precisa EXISTIR — se não existir, apenas retorna lista vazia — mas agora precisa ter FORMATO de domínio válido)." }],
    "query": [], "body": [], "headers": []
  },
  "respostas": [
    {
      "status": 200,
      "quando": "Sempre — lê apenas o arquivo local, não consulta o Stalwart nem exige o servidor rodando.",
      "corpo": { "mailboxes": "Mailbox[] (sem o campo password)" },
      "exemplo": { "mailboxes": [
        { "id": "postmaster@exemplo.com.br", "localPart": "postmaster", "domain": "exemplo.com.br", "kind": "system", "createdAt": "2026-08-01T12:00:00.000Z" },
        { "id": "contato@exemplo.com.br", "localPart": "contato", "domain": "exemplo.com.br", "kind": "user", "createdAt": "2026-08-10T09:30:00.000Z" }
      ] }
    }
  ],
  "erros": [
    { "status": 400, "codigo": "invalid_request", "quando": "O segmento :domain reprovado pelo schema Fastify (domainParamSchema) ANTES do handler: fora do padrão de domínio ou maior que 253 chars.", "mensagem": "Campo inválido: domain." }
  ],
  "fluxo": [
    "1. normalizeMailDomain(domainName) (não valida existência do domínio, só normaliza).",
    "2. Filtra data.mailboxes por domain === o normalizado e remove o campo password de cada item antes de devolver."
  ],
  "dependencias": [
    { "tipo": "servico", "nome": "MailService.listMailboxes", "arquivo": "apps/server/src/services/mail-service.ts:337", "papel": "Filtra e mascara (remove password) as caixas do domínio." }
  ],
  "efeitosColaterais": ["Nenhum (somente leitura)."],
  "chamadoPor": ["apps/web/src/pages/MailDomainPage.tsx:199"],
  "testes": ["scripts/test-mail.mts"],
  "observacoes": [
    "Diferente da maioria das outras rotas de domínio, esta NÃO retorna 404 domain_not_found se o domínio não existir — apenas devolve uma lista vazia, pois normalizeMailDomain só valida o formato, não a existência.",
    "MUDANÇA DE COMPORTAMENTO: antes desta rota tolerar qualquer string em :domain (mesmo fora do formato de domínio, já que só filtra em memória sem exigir existência), agora o schema Fastify recusa formato inválido com 400 invalid_request antes de chegar ao handler."
  ]
}
