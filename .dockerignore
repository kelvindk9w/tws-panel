# Versionamento / metadados
.git
.gitattributes
.gitignore
.github

# Dependências e builds locais (reconstruídos dentro da imagem)
node_modules
**/node_modules
apps/web/dist
**/*.tsbuildinfo

# Dados de runtime (ficam no volume paas_data)
data

# Arquivos de ambiente / segredos
.env
.env.*

# Docs e exemplos não são necessários no runtime
docs
examples
*.md
!README.md

# Compose e scripts de dev
docker-compose.yml
docker-compose.dev.yml

# Logs / SO
*.log
.DS_Store
.idea
.vscode
